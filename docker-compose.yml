version: "3.10"
services:
  app:
    build:
      context: .
    image: atuljkamble/nodejs:latest
    container_name: nodejsapp
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MESSAGE=Hello from Compose!
    init: true
    restart: unless-stopped
